// [5, 8, 2, 9, 1]
// we need to reverse the array

function reverseArray(arr) {

    // running the loop for array from backward
    for (let i = arr.length - 1; i >= 0; i--) {
        // printing 
        console.log(arr[i])
    }

}

reverseArray([5, 8, 2, 9, 1])
